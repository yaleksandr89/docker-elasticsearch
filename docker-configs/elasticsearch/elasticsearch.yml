cluster.name: "global-cluster"
network.host: 0.0.0.0
xpack.security.enabled: false

#Что делает network.host: 0.0.0.0?
#  Elasticsearch по умолчанию слушает только localhost (недоступен извне).
#  0.0.0.0 говорит: "Принимай соединения со всех IP-адресов".
#  Это позволяет другим сервисам, например nginx, обращаться к Elasticsearch.
